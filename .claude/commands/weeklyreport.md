---
description: Generate weekly task report from git log
---

Summarize this week's work: $ARGUMENTS

Look at the git log in this repo, filtered to commits by my GitHub username 
"mustafabutt-dev" only, within that date range. List the tasks completed — 
grouped by feature/area, not commit-by-commit. Just the task list, nothing else.

Format as clean HTML — use <strong> for a header if needed, no <p>/<br> tags, 
just blank lines between items (this will be pasted into a WordPress comment). 
Save the output as weekly-report-draft.html in this repo. Do not publish or 
post anywhere — this is a draft for me to review.
