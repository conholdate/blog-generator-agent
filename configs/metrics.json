{
  "content_gap_agent": {
    "agent_name": "Content Gap Agent",
    "agent_owner": "Muzammil Khan",
    "stages": [
      "Content Coverage Map",
      "Content Gap Analysis"
    ],
    "webhooks": {
      "primary": {
        "url": "${METRICS_PRIMARY_WEBHOOK_URL}",
        "token": "${METRICS_PRIMARY_WEBHOOK_TOKEN}"
      },
      "internal": {
        "url": "${METRICS_INTERNAL_WEBHOOK_URL}",
        "token": "${METRICS_INTERNAL_WEBHOOK_TOKEN}"
      }
    }
  },
  "content_indexer_agent": {
    "agent_name": "Content Indexer Agent",
    "agent_owner": "Muzammil Khan",
    "stages": [
      "Content Indexing"
    ],
    "webhooks": {
      "primary": {
        "url": "${METRICS_PRIMARY_WEBHOOK_URL}",
        "token": "${METRICS_PRIMARY_WEBHOOK_TOKEN}"
      },
      "internal": {
        "url": "${METRICS_INTERNAL_WEBHOOK_URL}",
        "token": "${METRICS_INTERNAL_WEBHOOK_TOKEN}"
      }
    }
  },
  "blog_keyword_analyzer": {
    "agent_name": "Blog Keyword Analyzer",
    "agent_owner": "Muzammil Khan",
    "stages": [
      "Keyword Clustering",
      "Topics Generation"
    ],
    "webhooks": {
      "primary": {
        "url": "${METRICS_PRIMARY_WEBHOOK_URL}",
        "token": "${METRICS_PRIMARY_WEBHOOK_TOKEN}"
      },
      "internal": {
        "url": "${METRICS_INTERNAL_WEBHOOK_URL}",
        "token": "${METRICS_INTERNAL_WEBHOOK_TOKEN}"
      }
    }
  }
}